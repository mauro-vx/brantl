import NavMenu from "@/components/ui/nav-menu";

export default function Header() {
  return (
    <header className="container fixed left-0 right-0 top-0 z-50 bg-transparent text-white">
      <NavMenu />
    </header>
  );
}
